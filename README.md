# Telegram Todo-List Bot

Простой персональный telegram-бот для управления списком задач, написанный на Python с использованием aiogram.

## Возможности

- ✅ Добавление задач
- 📋 Просмотр всех задач
- ✔️ Отметка задач как выполненных
- 🗑️ Удаление задач
- 🧹 Очистка выполненных задач
- 💾 Постоянное хранилище (SQLite)

## Команды бота

- `/start` - Приветствие и справка
- `/add <задача>` - Добавить новую задачу
- `/list` - Показать все задачи
- `/done <id>` - Отметить задачу выполненной
- `/delete <id>` - Удалить задачу
- `/clear` - Удалить все выполненные задачи

## Установка и запуск

### Шаг 1: Получение токена бота

1. Откройте Telegram и найдите [@BotFather](https://t.me/botfather)
2. Отправьте команду `/newbot`
3. Следуйте инструкциям для создания нового бота
4. Скопируйте полученный токен

### Шаг 2: Настройка окружения

1. Скопируйте файл `.env.example` в `.env`:
```bash
cp .env.example .env
```

2. Откройте `.env` и вставьте ваш токен:
```
BOT_TOKEN=your_actual_bot_token_here
```

### Шаг 3: Запуск с Docker Compose

```bash
docker-compose up -d
```

Бот запустится в фоновом режиме. Для просмотра логов:

```bash
docker-compose logs -f
```

Для остановки бота:

```bash
docker-compose down
```

## Локальный запуск (без Docker)

Если вы хотите запустить бота локально без Docker:

1. Установите Python 3.11 или выше
2. Установите зависимости:
```bash
pip install -r requirements.txt
```

3. Запустите бота:
```bash
python bot.py
```

## Структура проекта

```
.
├── bot.py              # Главный файл бота
├── handlers.py         # Обработчики команд
├── database.py         # Работа с базой данных
├── requirements.txt    # Python зависимости
├── Dockerfile         # Docker конфигурация
├── docker-compose.yml # Docker Compose конфигурация
├── .env.example       # Пример файла окружения
└── README.md          # Документация
```

## Автоматический деплой

Проект настроен для автоматического развертывания на сервер Timeweb через GitHub Actions.

### Первоначальная настройка

1. **Установите Docker на сервере Timeweb** - следуйте инструкциям в [DOCKER_SETUP.md](DOCKER_SETUP.md)
2. **Настройте GitHub Secrets** - следуйте инструкциям в [.github/workflows/SETUP_SECRETS.md](.github/workflows/SETUP_SECRETS.md)
3. **Клонируйте репозиторий на сервер:**
   ```bash
   cd ~
   git clone https://github.com/ваш-username/eclipse-todolist-bot.git
   cd eclipse-todolist-bot
   ```

### Автоматическое обновление

После настройки каждый push в ветку `main` автоматически:
- Подключается к вашему серверу через SSH
- Скачивает последние изменения
- Пересобирает Docker контейнер
- Перезапускает бота с новым кодом

Статус деплоя можно отслеживать во вкладке **Actions** в GitHub.

## База данных

Бот использует SQLite для хранения задач. При запуске через Docker Compose база данных сохраняется в директории `./data`, что обеспечивает сохранность данных при перезапуске контейнера.

## Технологии

- **Python 3.11** - Язык программирования
- **aiogram 3.x** - Асинхронный фреймворк для Telegram Bot API
- **aiosqlite** - Асинхронная работа с SQLite
- **Docker & Docker Compose** - Контейнеризация
- **GitHub Actions** - CI/CD для автоматического деплоя
