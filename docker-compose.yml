version: '3.8'

services:
  bot:
    build: .
    container_name: todolist-bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./data:/data
    environment:
      - PYTHONUNBUFFERED=1
