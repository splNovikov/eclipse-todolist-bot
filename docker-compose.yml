services:
  bot:
    build: .
    container_name: todolist-bot
    restart: unless-stopped
    volumes:
      - ./data:/data
    environment:
      - PYTHONUNBUFFERED=1
      - BOT_TOKEN=${BOT_TOKEN}
